var mongoose = require('mongoose'),
  Schema = mongoose.Schema;

  var movieSchema=[]; //is this where to insert api-linking





var Movie = mongoose.model('Movie', movieSchema);
module.exports = Movie;
